# Requirements
    Node = 12

# Installation Steps
    > cp .env.example .env
    > set NODE_ENV in .env file
    > set MAIL_SERVICE=aws
    

# Other Configuration
    > Update config as per NODE_ENV if required
    > config/index.js